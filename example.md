# Python 数据分析实战笔记
## 从数据清洗到可视化（md-latex-pdf 示例文档，演示全部支持的语法）

页眉：Python 数据分析 · 实战笔记
页尾：本文档为 md-latex-pdf 示例，运行 `python md_latex_pdf.py example.md` 即可生成 PDF
作者：示例作者
版本：v1.2
日期：2025-01-01
封面：#FFFFFF
---

> [!IMPORTANT] 使用说明
> 这份示例把工具支持的 Markdown 语法都写了一遍：封面 front matter、章节自动编号、表格、代码块高亮、嵌套列表与任务列表、引用、脚注、数学公式、告示框、强制分页、图片。
> 公式由 LaTeX 渲染，不需要 KaTeX。

# 01. 数据读取与清洗

## 读取 CSV

读文件用标准库或 pandas 都行。编码和缺失值先看清楚，后面少返工。这一段顺带演示行内强调：**加粗**、*斜体*、~~删除线~~、行内代码 `pd.read_csv`，还有需要转义的特殊字符：50% 完成、a_b、C++、{花括号}、# 号、$ 符号。

```python
import pandas as pd

df = pd.read_csv("sales.csv", encoding="utf-8-sig")
df = df.dropna(subset=["amount"])
df["date"] = pd.to_datetime(df["date"])
print(df.head())
```

## 常用操作对比

| 操作 | 方法 | 说明 | 备注 |
| :--- | :---: | ---: | :---: |
| 读取 | `read_csv` | 读取 CSV 文件 | 常用 |
| 筛选 | `query` | 按条件过滤行 | 高效 |
| 分组 | `groupby` | 聚合统计 | 重点 |
| 透视 | `pivot_table` | 行列重排 | 进阶 |

## 数据清洗要点

顺序基本固定：

1. 先跑 `df.info()` 和 `df.describe()`，看清字段和分布
2. 处理缺失和重复
3. 统一数据类型
4. 结果另存新文件，原始数据别覆盖

### 缺失值

`dropna` 删行，`fillna` 补值，两条路各有代价。

#### 删行

样本量会掉。删之前把整列的缺失比例算出来。

#### 补值

均值、中位数、众数都用得上，选哪个看分布。

##### 均值填充

算得快，容易被极端值带偏。

##### 中位数填充

抗异常值，代价是分布形状被抹平。

##### 众数填充

分类列常见做法。

###### 填 0 之前想一下

0 在金额、时长这类列里是有意义的取值，填进去等于造数据。

### 重复值与异常值

- 重复值：`drop_duplicates` 按关键列去重
  - 关键列先想清楚
  - 保留第一条还是最后一条
- 异常值：3σ 原则或 IQR 方法剔除

## 任务清单示例

- [x] 环境安装与依赖确认
- [x] 数据读取脚本
- [ ] 清洗规则写成文档
- [ ] 自动化流水线

## 引用与脚注

> 数据清洗占掉分析流程的大头，六成时间花在这上面不算多。

有人建议把清洗规则写成配置项[^1]，方便复现。公式示例：质能方程 $E = mc^2$，以及更复杂的 $f(x) = \frac{1}{\sqrt{2\pi}\sigma} e^{-\frac{(x-\mu)^2}{2\sigma^2}}$。

$$S = \sum_{i=1}^{n} x_i^2, \quad \bar{x} = \frac{1}{n}\sum_{i=1}^{n} x_i$$

## 数学符号（正文直写即可）

中文字体缺字形的常用符号已经内置了回退，正文里直接写，不用包 `$...$`：对 ∀ε>0 存在 ∃δ>0；A ⊂ B ⊆ C；x ↦ y；a ⇔ b；空集 ∅、偏导 ∂、梯度 ∇、张量积 ⊗；角标 10⁻³、m³、v₀、a₁、x²+y²；x∈ℝ、⌈x⌉≥x；✓ 通过 ✗ 未通过。

| 类别 | 符号 |
|:---|:---|
| 逻辑 / 集合 | ∀ ∃ ∄ ∉ ∋ ⊄ ⊇ ⊊ ⊋ ∖ |
| 箭头 | ⇐ ⇒ ⇔ ↦ ⇀ ⇁ ↔ ↕ |
| 运算 | ∬ ∓ ⋅ ∘ ∗ ∙ ≃ ≅ ≪ ≫ ⊖ ⊘ ⋯ ⋮ |
| 罗马数字 / 图形 | Ⅰ Ⅱ Ⅲ Ⅳ ■ □ ▲ △ ▼ ▽ ◆ ◇ ○ ◎ ● ★ ☆ |

> [!TIP]
> 表里没收录的冷僻符号照旧可以写成 `$\gtrsim$` 这样的数学式。确实常用的，加进 `md_latex_pdf.py` 的 `SYMBOL_FALLBACKS` 就行。

## 化学式与单位（mhchem 与 siunitx）

单位交给 siunitx，行内 `\si` 和 `\unit` 都能用：

$$c = \frac{n}{V} = \si{\mol\per\litre}, \qquad a = \unit{\metre\per\second\squared}$$

化学式、离子式用 mhchem：$\ce{2H2 + O2 -> 2H2O}$、$\ce{SO4^2-}$、$\ce{CH3COOH <=> CH3COO- + H+}$。

浓度这种「化学式套单位」的写法也直写：$c(\ce{NaCl}) = \si{0.9\gram\per\litre}$。

## 表情与警示标记

正文里可以直接写 ❗ ⚠ 💡（带变体选择符的 ❗️ ⚠️ 💡️ 效果相同），PDF 里显示成对应图标。

引用块以标记开头，渲染出来是一块带底色的引述标记：

> 💡 思路
> 先看数据分布，再决定清洗策略；分布偏得厉害就优先中位数。

> ⚠ 单位换算最容易出错，逐项核对一遍再往下算。

> ❗ 易错提醒
> `dropna` 之后索引不连续，取行用 `iloc`，`loc` 会按标签走。

## 图片

![数据清洗流程图（示例图片）](example.png)

> [!TIP] 小提示
> `groupby` 之后记得 `reset_index()`，不然列结构变成索引，链式赋值会报警告。

> [!WARNING] 注意事项
> `fillna` 默认按列填。跨列填要显式传字典，或者给 `axis=1`，否则容易静默出错。

::page

# 02. 可视化与输出

## 绘图三步法

1. 选图形：折线看趋势，柱状比大小，散点找关系
2. 削减字段，只留要画的列
3. 出图后标注标题、轴标签、图例

## 配置片段

```json
{
  "output": "report.pdf",
  "dpi": 300,
  "style": "seaborn-v0_8",
  "savefig": { "bbox_inches": "tight" }
}
```

## 相关资源

- 官方文档：<https://pandas.pydata.org/docs/>
- 自动链接测试：https://matplotlib.org/stable/ 与 [Pandas 中文教程](https://pandas.pydata.org/docs/getting_started/index.html)

---

*这份 PDF 由 md-latex-pdf 转换得到：LaTeX 排版，版式紧凑，封面纯色。*

[^1]: 清洗规则写进配置，换人接手时不用重新问一遍。
