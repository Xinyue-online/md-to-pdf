# Python 数据分析实战笔记
## 从数据清洗到可视化（md-to-pdf 示例文档，用于验证全部语法）

页眉：Python 数据分析 · 实战笔记
页尾：本文档为 md-to-pdf 示例，运行 `md-to-pdf example.md` 即可生成 PDF
作者：示例作者
版本：v1.2
日期：2025-01-01
封面：#FFFFFF
---

::key 使用说明
本文演示 md-to-pdf 的全部特性：封面 front matter、LaTeX 自动章节编号、表格、代码块高亮、嵌套与任务列表、引用、脚注、数学公式、要点框、强制分页、图片。公式由 LaTeX 原生渲染，不需要 KaTeX。
::end

# 01. 数据读取与清洗

## 读取 CSV

读取文件用标准库或 pandas 都行，注意编码与缺失值。下面这段演示行内强调：**加粗**、*斜体*、~~删除线~~、行内代码 `pd.read_csv`，以及需要转义的特殊字符：50% 完成、a_b、C++、{花括号}、# 号、$ 符号。

```python
import pandas as pd

df = pd.read_csv("sales.csv", encoding="utf-8-sig")
df = df.dropna(subset=["amount"])
df["date"] = pd.to_datetime(df["date"])
print(df.head())
```

## 常用操作对比

| 操作 | 方法 | 说明 | 备注 |
| :--- | :---: | ---: | :---: |
| 读取 | `read_csv` | 读取 CSV 文件 | 常用 |
| 筛选 | `query` | 按条件过滤行 | 高效 |
| 分组 | `groupby` | 聚合统计 | 重点 |
| 透视 | `pivot_table` | 行列重排 | 进阶 |

## 数据清洗要点

- 缺失值：`dropna` / `fillna` 二选一
  - 直接删除行 → 样本量损失
  - 均值/中位数填充 → 引入偏差
- 重复值：`drop_duplicates` 按关键列去重
- 异常值：3σ 原则或 IQR 方法剔除

按步骤执行：

1. 先探查 `df.info()` 与 `df.describe()`
2. 再处理缺失与重复
3. 最后统一数据类型
4. 校验结果写入新文件

## 任务清单示例

- [x] 环境安装与依赖确认
- [x] 数据读取脚本
- [ ] 清洗规则文档化
- [ ] 自动化流水线

## 引用与脚注

> 数据清洗是整个分析流程中最耗时的一步，通常占全部工作量的 60% 以上。

有人建议把清洗规则写成配置项[^1]，以便复现与审计。公式示例：质能方程 $E = mc^2$，以及更复杂的 $f(x) = \frac{1}{\sqrt{2\pi}\sigma} e^{-\frac{(x-\mu)^2}{2\sigma^2}}$。

$$S = \sum_{i=1}^{n} x_i^2, \quad \bar{x} = \frac{1}{n}\sum_{i=1}^{n} x_i$$

## 数学符号（正文直写即可）

中文字体缺字形的常用符号已经内置回退，正文里直接写就行，不必包 `$...$`：对 ∀ε>0 存在 ∃δ>0；A ⊂ B ⊆ C；x ↦ y；a ⇔ b；空集 ∅、偏导 ∂、梯度 ∇、张量积 ⊗；角标 10⁻³、m³、v₀、a₁、x²+y²；x∈ℝ、⌈x⌉≥x；✓ 通过 ✗ 未通过。

| 类别 | 符号 |
|:---|:---|
| 逻辑 / 集合 | ∀ ∃ ∄ ∉ ∋ ⊄ ⊇ ⊊ ⊋ ∖ |
| 箭头 | ⇐ ⇒ ⇔ ↦ ⇀ ⇁ ↔ ↕ |
| 运算 | ∬ ∓ ⋅ ∘ ∗ ∙ ≃ ≅ ≪ ≫ ⊖ ⊘ ⋯ ⋮ |
| 罗马数字 / 图形 | Ⅰ Ⅱ Ⅲ Ⅳ ■ □ ▲ △ ▼ ▽ ◆ ◇ ○ ◎ ● ★ ☆ |

> [!TIP]
> 表里没收录的冷僻符号仍可写成 `$\gtrsim$` 这样的数学式；确实常用的可以加进 `md_to_pdf.py` 的 `SYMBOL_FALLBACKS`。

## 表情与警示标记

正文里可以直接写 ❗ ⚠ 💡（带变体选择符的 ❗️ ⚠️ 💡️ 效果相同），PDF 里显示成对应图标。

以标记开头的引用块会自动变成彩色告示框：

> 💡 思路
> 先看数据分布，再决定清洗策略；分布严重偏斜时优先考虑中位数。

> ⚠ 单位换算最容易出错，务必逐项核对一遍再往下算。

> ❗ 易错提醒
> `dropna` 之后索引不连续，取行要用 `iloc` 而不是 `loc`。

## 图片

![数据清洗流程图（示例图片）](example.png)

::tip 小提示
`groupby` 之后记得用 `reset_index()` 恢复列结构，避免链式索引警告。
::end

::warn 注意事项
`fillna` 默认按列填充，跨列填充需显式传入字典或使用 `axis=1`，否则容易静默出错。
::end

::page

# 02. 可视化与输出

## 绘图三步法

1. 确定图形类型（折线/柱状/散点）
2. 精简数据字段（只保留需要的列）
3. 输出与标注（标题、轴标签、图例）

## 配置片段

```json
{
  "output": "report.pdf",
  "dpi": 300,
  "style": "seaborn-v0_8",
  "savefig": { "bbox_inches": "tight" }
}
```

## 相关资源

- 官方文档：<https://pandas.pydata.org/docs/>
- 自动链接测试：https://matplotlib.org/stable/ 与 [Pandas 中文教程](https://pandas.pydata.org/docs/getting_started/index.html)

---

*本文档由 md-to-pdf 生成，LaTeX 风格、紧凑排版、纯色封面。*

[^1]: 配置化清洗规则便于团队协作与规则复用。
