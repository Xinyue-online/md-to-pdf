# md-to-pdf 输入 Markdown 写作注意事项

> 适用版本：md-to-pdf **v1.0.6**（本仓库根目录）。本文档约束「写/改写会被 md-to-pdf 转换的 .md 讲义」时要注意的事项；转换命令、参数与产物验证见 [README.md](README.md)。示例均经 md-to-pdf 实测。

## 总原则

- md-to-pdf 是**自定义解析器**（非 GitHub/CommonMark 全集）：只用本文列出的语法，原生 HTML 表格等会被忽略。
- 产出 .md 前过一遍文末「输出前自检清单」；转换后按 README「生成后的校验」检查 0 Overfull / 0 Missing（v1.0.5 起缺字形会自动作为警告输出，不必手动翻 .log）。

## 封面 front matter（文档开头）

写法（行式，以**单独一行 `---` 结束**）：

```markdown
# 文档标题
## 副标题（可省略）

页眉：页眉文字（默认取标题）
页尾：封面底部说明文字
封面：#EAF2FB（封面底色，默认白 #FFFFFF）
作者：张三
版本：v1.0
---
```

**特殊关键词**（其余任意键一律按「键：值」显示成封面条目）：

| 关键词 | 含义 |
| --- | --- |
| `页眉`（或 `header`） | 页眉文字，默认取标题 |
| `页尾`（旧名 `foot` / `footer` / `页脚` 兼容） | 封面底部小字说明 |
| `封面`（旧名 `cover` 兼容） | 封面纯色底色 `#RRGGBB`（必须 6 位 hex，否则回退白色） |
| `作者`、`版本` 等 | 封面「键：值」条目 |

易错点：

- 封面行的值**只放纯文字**——行内不做解析（标题/关键词值仅转义，`$公式$`/`**粗体**` 不会生效）。
- 隐式封面判定：前缀以 `# 标题` 开头、每非空行都是标题或 `键：值`、≤ 30 行。**正文里的 `---` 分隔线已不会误当封面**；但别在文件最前制造一个“像封面”的短前缀再跟 `---`，那会被解析成真封面。
- 不需要封面时直接写正文（会套默认「未命名文档」封面）。

## LaTeX 数学：一律用美元符包裹

- 行内：`$...$`；独立成段/多行：`$$...$$`（独占段落）。md-to-pdf **原生透传 LaTeX**，支持 `\frac`、`\sum`、`\text{}` 等完整语法。
- 常用宏已内置：`\dif`、`\dfrac`、`\alpha`、`\sigma` 等。
- **禁止** `\[ ... \]` / `\( ... \)`（不识别，会原样显示）。
- 文字里的真实美元符（价格 `$5`）要写 `\$5` 或放行内代码 `` `$5` ``，否则会被当公式起点。
- 粗体可嵌公式：`**安培力 $F_A$ 方向**`（已支持）。
- 正文想写希腊字母（3σ、均值 μ）可直接写（v1.0.3 起内置中文字体回退，不再缺字）；常用数学符号（∀ ∃ ∅ ⊂ ⊆ ⇒ ⇐ ⇔ ↦ ∂ ∇ ⊗、角标 ⁴⁻ ₀₁ 等）v1.0.5 起也已内置回退，直接写即可（详见下节）。

## 正文符号：能直写哪些（v1.0.5 起，表情标记 v1.0.6 起）

中文字体只覆盖一部分符号，没字形又没兜住的符号直接写正文会变成**空白**（xelatex 只在 .log 里报 `Missing character`，PDF 上少一块，不报错也不中断）。v1.0.5 起内置了两条路、v1.0.6 起再加一条，对写作者都是透明的：

| 情况 | 写法 | 例子 |
| --- | --- | --- |
| 中文字体本来就有字形 | 直接写正文 | `3σ`、`x∈A`、`∑`、`a≤b`、`±`、`①②③` |
| 中文字体有、西文字体没有（导言区字符类声明） | 直接写正文 | 罗马数字 `Ⅰ Ⅱ Ⅲ Ⅻ ⅰ`、`′ ″`、`■ □ ▲ △ ▼ ▽ ◆ ◇ ○ ◎ ● ★ ☆ ♀ ♂`、制表符 `─ │ ┌ ┐ └ ┘ ├ ┤ ┼`、`▁ ▂ █ ▌`、`℅ ℉ ℡ ― ⌒` |
| 两个字体都没有、但有 LaTeX 写法（`SYMBOL_FALLBACKS` 153 个） | 直接写正文，不必包 `$...$` | `∀ε>0`、`A ⊂ B ⊆ C`、`x ↦ y`、`a ⇔ b`、`10⁻³`、`⌈x⌉`、`x∈ℝ`、`✓ ✗ ☑`、`⅓ ⅔`、`Ⅼ Ⅽ`、`⑪ ⑫` |
| 表情/警示标记（`EMOJI_ICONS`：切到 Segoe UI Symbol） | 直接写正文 | `❗`(U+2757) `⚠`(U+26A0) `💡`(U+1F4A1)；带变体选择符的 `❗️ ⚠️ 💡️` 写成一样的效果（那个零宽的 U+FE0F 会被自动删掉） |
| 三条路都没收录的冷僻符号 | 用 `$...$` 包数学命令 | `$\gtrsim$`、`$\nvdash$` |
| 常用但没收录 | 加进 `SYMBOL_FALLBACKS`/`EMOJI_ICONS`（或按 `measure_coverage.py` 输出补字符类声明），再跑 `python test_symbol_fallback.py` | — |

要点：

- **公式里不受影响**：`$∀$`、`$\Leftarrow$`、`$\varnothing$` 照旧正确（回退用的是 `\ensuremath`，幂等）；`$❗$` 也与正文写法一致。
- 回退表覆盖：逻辑 `∀ ∃ ∄`；集合 `∅ ∉ ∋ ∌ ⊂ ⊃ ⊄ ⊆ ⊇ ⊊ ⊋ ∖`；箭头 `↔ ↕ ⇐ ⇒ ⇔ ↦ ⇀ ⇁`；运算 `∬ ∂ ∇ − ∓ ⋅ ∘ ∗ ∙ ≃ ≅ ≪ ≫ ≺ ≻ ⊢ ⊨ ⊖ ⊗ ⊘ ⋯ ⋮ ⌈ ⌉ ⌊ ⌋ ∎ ∐ ∭ ∡ ∢ ∼ ≲ ≳ ≰ ≱ ≢ ⊈ ⊉`；数学字母 `ℝ ℕ ℤ ℚ ℂ ℙ ℍ ℓ ℏ ℘ ℵ ℑ ℜ`；角标 `⁰¹²³⁴⁵⁶⁷⁸⁹⁺⁻⁼⁽⁾ⁿ`、`₀₁₂₃₄₅₆₇₈₉₊₋₌₍₎`；勾叉 `✓ ✔ ✗ ✘`；分数 `⅓ ⅔ ⅕…⅞`；表情标记 `❗ ⚠ 💡`。
- **表情标记依赖 Windows 自带的 `Segoe UI Symbol`**：非 Windows 机器上通常没有该字体，此时 `❗ ⚠ 💡` 会被丢弃（不留空白格、不报错）——跨平台文稿建议改用 `> [!TIP]` 式的文字表述。
- 页眉、封面（front matter 值）、表格单元格、告示框、行内代码里的符号同样生效。
- **已知限制**：PDF 书签与文档属性（元数据）里的符号会被 hyperref 省略（`Token not allowed in a PDF string` 警告），只影响书签文字，不影响页面显示。
- 转换后想确认没有漏网之鱼：加 `--keep-aux` 后查 `.log` 里 `Missing character` 条数应为 0（v1.0.5 起也会自动作为警告打印出来）。

## 标题：不手写编号、不重名

- 自动编号（#→1、##→1.1…），md 里别写 `# 第1章`、`## 1.1 xxx`（会被剥离，但约定就是别写）。
- 两个标题文字完全相同 → hyperref 重复目标警告：写标题时区分。
- **编号起点**：封面的一级标题（`# 封面标题`）**不计数**。若正文没有 `#` 一级标题（被封面吸收），md-to-pdf 把整篇当作单一"章"，封面外**第一个 `##` 编号为 1.1**（其后 1.2、1.3…），其下 `###` 为 1.1.1、1.1.2…——既不是 0.1、0.2…，也不会把 `##` 提升成单独"1"。习惯做法：正文章节用 `#`、二级用 `##`，最直观。
- **目录只收几级**是转换选项，不是 md 语法：CLI 用 `--toc-depth N`(1–4) / `--no-toc`，GUI 用「目录层级」下拉（无目录 / 1–4 级，默认 3 级）；它只影响目录页，不改变正文章节编号。

## 引号

- 中文段落里：全角弯引号 “ ” ‘ ’ 原样全角渲染；半角直引号 `"…"` / `'…'` 包中文时**自动转中文弯引号**。
- 纯英文段落 / 行内代码 / 代码块内不转换；单词内撇号（don't）、落单引号（英寸 5"）不受影响。

## 告示框 / 要点框

三种写法，选一种用（都在 `>` 引述里，互不冲突）：

**① GitHub 风格（推荐，语义最全）**

```markdown
> [!WARNING]
> 最容易漏的是安培力
> 最容易漏的是安培力，最容易画错方向的也是安培力。
```

- 类型与配色：`NOTE` 蓝 / `TIP` 绿 / `IMPORTANT` 紫 / `WARNING` 橙 / `CAUTION` 红；大小写不敏感；缺省标题 注意/提示/重要/警告/小心。
- 自定义标题：`> [!TIP] 快速检查`——标题与 `[!类型]` **同一行**；标题另起一行会被当正文渲染。
- **相邻告示框之间必须空行分隔**；框内容支持粗体、公式、行内代码、列表、多段。
- 按语义选型：补充说明用 NOTE，易错用 WARNING/CAUTION，重点记忆用 IMPORTANT。

**② 引述标记（v1.0.6 起，写起来最省事）**

```markdown
> 💡 思路
> 先画受力图，再判断含安培力的方向。

> ⚠ 单位换算最容易出错，务必逐项核对一遍再往下算。

> ❗ 易错提醒
> 磁通量变化率要取绝对值。
```

- 对应关系：`❗`→红「注意」、`⚠`→橙「警告」、`💡`→绿「提示」（与 CAUTION / WARNING / TIP 同色）。
- **标题规则**：标记行剩下的文字够短（≤16 显示宽度、不以 `。！？!?…` 结尾）就当框标题（`> 💡 思路` → 绿框「思路」）；否则标题用缺省名，那段话连同标记留在正文里。
- 标记后面空格可有可无（`> ❗ 易错` = `> ❗易错`）；只认**首行**的标记，`> ⚠️` 里的变体选择符会自动删掉，不影响识别。
- 两个提醒：**相邻告示框之间同样要空行分隔**；标记行若写了长句，别指望它成为标题（会被当正文，这是有意的）。

**③ 普通引用（不加标记，自动套中性框）**

```markdown
> 数据清洗是整个分析流程中最耗时的一步，通常占全部工作量的 60% 以上。
```

- v1.0.6 起，不带标记的 `> 引用` 渲染成**浅灰底 + 左侧竖条**的中性框（可跨页），一眼能把"引述别人的话"与自己的话分开——**不必加任何标记**，直接写就行。
- 支持多段（`>` 空行 `>`）、列表、公式、粗体，内部再嵌一层引用也正常；孤零零一个 `>` 不输出任何东西。
- 想让它变成彩色框，就在首行加 `❗ ⚠ 💡` 或改用 `> [!TYPE]`。

**禁用旧式写法**（2026-09-11 起）：`::tip` / `::warn` / `::key` … `::end` 只在解析器里作向后兼容保留（示例见 `example.md`），**新写的 md 一律不用**。语义对应 TIP=::tip、WARNING=::warn、IMPORTANT=::key。

## 表格

- 用 Markdown 表格，**第二行对齐行** `|:---|:---:|:--|` 必须有。
- 长文本列自动换行（≥24 显示宽度转 tabularx）；>24 行自动转 longtable 跨页；单元格内容做行内解析。
- 别用原生 HTML `<table>`（忽略并告警）；避免超长不可断词（Overfull 多为行尾碎片，调整措辞）。

## 代码

- 行内用反引号；多行用围栏 ```lang ```。高亮语言 python/c/cpp/java/sql/html/xml/go/lua/tex/ruby/php/matlab/bash 等；json/css/js/rust 无内置高亮但内容正常。
- 别输出嵌套三重反引号；代码里别出现 `\end{lstlisting}`（整块降级纯文本并告警）。

## 其它语法速查

- `**粗体**`、`*斜体*` / `_斜体_`、`~~删除线~~`；`- [ ]` / `- [x]` 任务列表；列表可嵌套。
- `> 引用`；`---` 分隔线；`::page`（单独一行）= 强制分页。
- `[文字](url)`、`<url>`、裸 URL → 链接；`![说明](本地图.png)` → 图片拷入输出 `images/`。
- 脚注：正文 `[^1]`，定义 `[^1]: 内容`（位置随意，预扫描）。

## 在 md 里画"盖在公式上的箭头"（线桥 / 电子转移桥 / 标注箭头）→ 用 `line-bridge`

`$$...$$` 的内容会原样进 `\[...\]`，而模板经 `tcolorbox` 已经带进 tikz，所以**不改本工具就能直接在 md 里画 tikz**——化学线桥（单线桥/双线桥）、受力箭头这类"锚在公式某个字符上的箭头"都这么做。

**写法与自检一律以同级工具目录的 `line-bridge\README.md` 为准**（工具：`gen` 生成图块 / `apply` 把 `![](images/xxx.png)` 换成图块 / `check` 量最终 PDF 自检 / `measure` 实测元素半宽；技能文档 `format-convert-router\references\docs\line-bridge.md`）。别在这里另抄一份版式参数。

只在本文件留下三条最容易踩、且与本工具强相关的约束：

1. **宏用 `\gdef`**：`$$…$$` → `\[…\]` 自成一组，块里 `\def` 出了这块就失效（后续各块会报 `Undefined control sequence`）。
2. **`\usetikzlibrary{…}` 不能在数学模式里执行**（`Missing $ inserted`）；别指望 `calc`，核心 tikz（`node[midway,above]`、`(x.north)`）够用。
3. **占位块必须留宽度**：overlay 箭头不占版面，而 `\rule{0pt}{…}` 零宽度**完全不参与版面高度**——写了等于没写，标签会压住上一段文字；`\rule[raise]{w}{h}` 的上沿在 `raise+h`，所以"上方留 A、下方留 B"要写 `\rule[-B]{..}{A+B}`（工具自动生成，别手改）。

## 输出前自检清单

1. 封面（若需要）：`# 标题` 开头 + 关键词 + 单独 `---`；底部说明用 **页尾**。
2. 所有数学都在 `$…$` / `$$…$$` 内，正文无裸 `$`（常用数学符号例外，见「正文符号：能直写哪些」）。
3. 标题无手写编号、无重复标题。
4. 提示/警告一律用 `> [!TYPE]` 或引述标记 `> ❗` / `> ⚠` / `> 💡`（自定义标题同行写，告示框之间空行）；旧式 `::tip/::warn/::key` 不写。
5. 表格有对齐行、无 HTML 表格；代码围栏配对。
6. 未把 `---` 放在“像封面”的前缀后（除非真想写封面）。
